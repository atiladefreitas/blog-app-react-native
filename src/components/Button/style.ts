import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 50,
    backgroundColor: "#5C7B9C",
    borderRadius: 4,
    flexDirection: "row",
    alignItems: "center",
  },
  title: {
    flex: 1,
    color: "#000",
    fontSize: 16,
    textAlign: "center",
  },
});
